1. Update `CombatStats` and `PlayerCombatStats` (in `entities/base.py` and `engine.py`) to have `body_parts_hp` and `body_parts_max_hp` dictionaries.
2. Implement localized damage logic. The user prompt says: "I like to do this for all body parts and you know continue to deepen our simulation. ... localized damage can tie directly into your NPC scheduling and needs system. If an NPC takes a critical hit to the leg, their move_cooldown or speed could drastically drop. In _update_npc_schedules(), you could add a high-priority check: if an NPC has a "broken_leg" status effect, they override their normal tasks and default to seeking_healer or resting_in_bed."
3. `CombatStats` should use `hp` and `max_hp` as properties or keep them in sync. Wait, the prompt says: "Entities access health and combat stats via their `combat` component (e.g., `player.combat.hp`), defined by `PlayerCombatStats` or `CombatStats`, which track localized damage via `body_parts_hp` and `body_parts_max_hp` dictionaries (`hp` and `max_hp` act as aggregate properties)." Let's make `hp` and `max_hp` properties in both `CombatStats` and `PlayerCombatStats` that aggregate the values from `body_parts_hp` and `body_parts_max_hp`.
4. In `engine.py`, `_update_npc_schedules`, add logic to check for "broken_leg" and other statuses (maybe just "broken_leg" for now as explicitly requested), overriding tasks to "seeking_healer" or "resting_in_bed".
5. Write tests and ensure test suite passes.
